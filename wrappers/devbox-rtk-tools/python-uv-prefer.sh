# Wrapper: python-uv-prefer
# Combines: environment management (devbox) + token optimization (RTK) + tool governance (prefer pip→uv)
# Note: devbox-manager.sh and rtk-wrapper.sh are combined at build time via Nix