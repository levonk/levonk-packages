# Wrapper: nodejs-yarn-force
# Combines: environment management (devbox) + token optimization (RTK) + tool governance (force npm/pnpm→yarn)
# Note: devbox-manager.sh and rtk-wrapper.sh are combined at build time via Nix