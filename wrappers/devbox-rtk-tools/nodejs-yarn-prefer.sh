# Wrapper: nodejs-yarn-prefer
# Combines: environment management (devbox) + token optimization (RTK) + tool governance (prefer npm/pnpm→yarn)
# Note: devbox-manager.sh and rtk-wrapper.sh are combined at build time via Nix