# Wrapper: nodejs-yarn-block
# Combines: environment management (devbox) + token optimization (RTK) + tool governance (block npm/pnpm)
# Note: devbox-manager.sh and rtk-wrapper.sh are combined at build time via Nix