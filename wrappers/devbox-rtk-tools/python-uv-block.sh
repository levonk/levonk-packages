# Wrapper: python-uv-block
# Combines: environment management (devbox) + token optimization (RTK) + tool governance (block pip)
# Note: devbox-manager.sh and rtk-wrapper.sh are combined at build time via Nix