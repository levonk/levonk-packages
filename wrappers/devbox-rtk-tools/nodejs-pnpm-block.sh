# Wrapper: nodejs-pnpm-block
# Combines: environment management (devbox) + token optimization (RTK) + tool governance (block npm)
# Note: devbox-manager.sh and rtk-wrapper.sh are combined at build time via Nix