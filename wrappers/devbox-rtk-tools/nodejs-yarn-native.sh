# Wrapper: nodejs-yarn-native
# Combines: environment management (devbox) + token optimization (RTK) + native tool usage
# Note: devbox-manager.sh and rtk-wrapper.sh are combined at build time via Nix